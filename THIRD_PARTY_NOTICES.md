# Third-party notices

The applicable MIT copyright and permission notice for reused foundation code
is retained in `LICENSE`.

Current direct Python dependency:

- `redis` / redis-py (MIT)

Test-only dependencies are `pytest` (MIT) and `pytest-asyncio` (Apache-2.0).

